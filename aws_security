1. SIEM - Security inform event management (Cloud Trail, Pub/sub).

2. IPS/IDS - AWS Guard Duty 

      (Intrusion Detection Systems (IDS) analyze network traffic for signatures that match known cyberattacks. 
      Intrusion Prevention Systems (IPS) also analyzes packets, 
      but can also stop the packet from being delivered based on what kind of attacks it detects — helping stop the attack).
      
3. SSO - Authentication (Octa tokens(identity pool - Google Authenticator), JWT token, Active directory/LDAP)
   https://aws.amazon.com/single-sign-on/
   
   SAML (Octo federaal protocol).
   
4. 

